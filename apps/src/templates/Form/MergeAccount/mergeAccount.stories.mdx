import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import {action} from "@storybook/addon-actions"
import MergeAccountView from "./MergeAccountView";

<Meta title="Form/Merge Account" component={MergeAccountView} />

# Merge Account

This view of MergeAccountView under `MergeAccountForm`
<Preview>
  <Story name="Merge Account">
    <MergeAccountView
      onSubmit="function"
      onSucces="function"
      onError="function"
      errorMessage="object"
    />
  </Story>
</Preview>

<br />

### How to Use
_MergeAccountView is just view element, logical form is created as `MergeAccountForm`. So, please read this before use._


```js
1. import MergeAccountForm
2. export MergeAccountForm(ViewComponent)
```

<br />

### Props
<Props of={MergeAccountView} />
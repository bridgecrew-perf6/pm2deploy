import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import {action} from "@storybook/addon-actions"
import ChangeEmailView from "./ChangeEmailView";

<Meta title="Form/Change Email" component={ChangeEmailView} />

# Change Email


This view of ChangeEmailView under `ChangeEmailForm`
<Preview>
  <Story name="Change Email">
    <ChangeEmailView
      onSubmit="function"
      onSucces="function"
      onError="function"
      errorMessage="object"
    />
  </Story>
</Preview>

<br />

### How to Use
_ChangeEmailView is just view element, logical form is created as `ChangeEmailForm`. So, please read this before use._


```js
1. import ChangeEmailForm
2. export ChangeEmailForm(ViewComponent)
```

<br />

### Props
<Props of={ChangeEmailView} />

import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import {action} from "@storybook/addon-actions"
import RegisterView from "./RegisterView";

<Meta title="Form/Register" component={RegisterView} />

# Merge Account

This view of RegisterView under `RegisterForm`
<Preview>
  <Story name="Register">
    <RegisterView
      onSubmit="function"
      onSucces="function"
      onError="function"
      errorMessage="object"
    />
  </Story>
</Preview>

<br />

### How to Use
_MergeAccountView is just view element, logical form is created as `RegisterForm`. So, please read this before use._


```js
1. import RegisterForm
2. export RegisterForm(ViewComponent)
```

<br />

### Props
<Props of={RegisterView} />